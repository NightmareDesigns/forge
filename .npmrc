# NPM security configuration for CraftForge

# Run npm audit on install
audit=true

# Fail install if critical vulnerabilities found
audit-level=moderate

# Use exact versions for dependencies (reproducible builds)
save-exact=true
